variables:
  CI_DESIGNATED_BRANCH: prplos

include:
  - local: .gitlab/build.yml
  - local: .gitlab/cdrouter.yml
  - local: .gitlab/coverity.yml
  - local: .gitlab/docker.yml
  - local: .gitlab/docker/builder/gitlab.yml
  - local: .gitlab/docker/testbed/gitlab.yml
  - local: .gitlab/docker/sdk/gitlab.yml
  - local: .gitlab/testbed.yml
  - local: .gitlab/testbed/nec-wx3000hp.yml
  - local: .gitlab/testbed/glinet-b1300.yml
  - local: .gitlab/testbed/turris-omnia.yml

stages:
  - docker
  - docker test
  - docker deploy
  - build
  - docker SDK
  - docker SDK test
  - docker SDK deploy
  - run
  - cdrouter
  - coverity

build test intel_mips prpl:
  extends: .build test config

build test ipq40xx prpl:
  extends: .build test config

build test mvebu prpl:
  extends: .build test config

coverity scan ipq40xx prpl:
  extends: .coverity scan config
  variables:
    CI_COVERITY_COMPILER_TEMPLATE_LIST: >
      arm-openwrt-linux-gcc
      arm-openwrt-linux-muslgnueabi-gcc

run test NEC WX3000HP NAND:
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends: .nec-wx3000hp testbed

run test Gl.iNet B1300 initramfs:
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends: .glinet-b1300 testbed

run test Turris Omnia initramfs:
  needs:
    - job: build test mvebu prpl
      optional: true
  extends: .turris-omnia testbed

CDRouter-Top-100 NEC NAND:
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter

CDRouter-Top-100 Gl.iNet initramfs:
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter

CDRouter-Top-100 Turris initramfs:
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter

CDRouter-Top-100 NEC VLAN WAN/LAN NAND:
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: vlan
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-vlans.sh

CDRouter-Top-100 Gl.iNet VLAN WAN/LAN initramfs:
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: vlan
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-vlans.sh

CDRouter-Top-100 Turris VLAN WAN/LAN initramfs:
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: vlan
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-vlans.sh

CDRouter-Top-100 NEC PPPoE WAN NAND:
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: ppp
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-pppoe.sh

CDRouter-Top-100 Gl.iNet PPPoE WAN initramfs:
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: ppp
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-pppoe.sh

CDRouter-Top-100 Turris PPPoE WAN initramfs:
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: ppp
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-pppoe.sh

CDRouter-IPv6-Top-100 NEC NAND:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test intel_mips prpl
      optional: true
  variables:
    CDROUTER_TEST_CONFIG: ppp-ipv6
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter

CDRouter-IPv6-Top-100 Gl.iNet initramfs:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  variables:
    CDROUTER_TEST_CONFIG: generic-ipv6
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter

CDRouter-IPv6-Top-100 Turris initramfs:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  variables:
    CDROUTER_TEST_CONFIG: generic-ipv6
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter

CDRouter-IPv6-Top-100 NEC VLAN WAN/LAN NAND:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: vlan-ipv6
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-vlans.sh

CDRouter-IPv6-Top-100 Gl.iNet VLAN WAN/LAN initramfs:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: vlan-ipv6
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-vlans.sh

CDRouter-IPv6-Top-100 Turris VLAN WAN/LAN initramfs:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: vlan-ipv6
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-vlans.sh

CDRouter-IPv6-Top-100 NEC PPPoE WAN NAND:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: ppp-ipv6
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-pppoe.sh

CDRouter-IPv6-Top-100 Gl.iNet PPPoE WAN initramfs:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: ppp-ipv6
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-pppoe.sh

CDRouter-IPv6-Top-100 Turris PPPoE WAN initramfs:
  allow_failure: true
  rules:
    - if: '$TFTP_IMAGE_DOWNLOAD_URL =~ $TFTP_IMAGE_FILENAME_PATTERN'
      when: always
    - if: '$TFTP_IMAGE_DOWNLOAD_URL'
      when: never
    - when: manual
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter
  variables:
    CDROUTER_TEST_CONFIG: ppp-ipv6
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-dut-enable-pppoe.sh

CDRouter-TR-069 NEC NAND:
  needs:
    - job: build test intel_mips prpl
      optional: true
  extends:
    - .nec-wx3000hp testbed
    - .cdrouter
  variables:
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-tr181-mockups.sh

CDRouter-TR-069 Gl.iNet NOR:
  needs:
    - job: build test ipq40xx prpl
      optional: true
  extends:
    - .glinet-b1300 testbed
    - .cdrouter
  variables:
    LABGRID_TARGET: glinet-b1300-nor
    TFTP_IMAGE_FILENAME: openwrt-ipq40xx-generic-glinet_gl-b1300-squashfs-sysupgrade.bin
    TFTP_IMAGE_FILENAME_PATTERN: /.*openwrt-ipq40xx-generic-glinet_gl-b1300-squashfs-sysupgrade.bin/
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-tr181-mockups.sh

CDRouter-TR-069 Turris eMMC:
  needs:
    - job: build test mvebu prpl
      optional: true
  extends:
    - .turris-omnia testbed
    - .cdrouter
  variables:
    LABGRID_TARGET: turris-omnia-emmc
    TFTP_IMAGE_FILENAME: openwrt-mvebu-cortexa9-cznic_turris-omnia-sysupgrade.img.gz
    TFTP_IMAGE_FILENAME_PATTERN: /.*openwrt-mvebu-cortexa9-cznic_turris-omnia-sysupgrade.img.gz/
    TFTP_IMAGE_UNPACK_COMMAND: "gunzip --force $TESTBED_TFTP_PATH/$TFTP_IMAGE_FILENAME || true"
    DUT_INIT_SCRIPTS: |
      .gitlab/scripts/dut-init/cdrouter-tr181-mockups.sh
