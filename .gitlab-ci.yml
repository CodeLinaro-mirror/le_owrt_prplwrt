variables:
  CI_DESIGNATED_BRANCH: prpl/nightly
  CI_DESIGNATED_BRANCH_SLUG: prpl-nightly

include:
  - local: .gitlab/build.yml
  - local: .gitlab/release.yml
  - local: .gitlab/cdrouter.yml
  - local: .gitlab/prplmesh.yml
  - local: .gitlab/cdrouter/prpl-haze.yml
  - local: .gitlab/cdrouter/turris-omnia.yml
  - local: .gitlab/cdrouter/mxl25641-hdk-6.yml
  - local: .gitlab/cdrouter/arcadyan-mozart.yml
  - local: .gitlab/cdrouter/wnc-freedom.yml
  - local: .gitlab/coverity.yml
  - local: .gitlab/docker.yml
  - local: .gitlab/docker/builder/gitlab.yml
  - local: .gitlab/docker/testbed/gitlab.yml
  - local: .gitlab/docker/sdk/gitlab.yml
  - local: .gitlab/docker/ib/gitlab.yml
  - local: .gitlab/docker/ib/lcm/gitlab.yml
  - local: .gitlab/testbed.yml
  - local: .gitlab/testbed/prpl-haze.yml
  - local: .gitlab/testbed/turris-omnia.yml
  - local: .gitlab/testbed/arcadyan-mozart.yml
  - local: .gitlab/testbed/mxl25641-hdk-6.yml
  - local: .gitlab/testbed/wnc-freedom.yml

stages:
  - docker
  - docker test
  - docker deploy
  - build
  - release
  - docker SDK and IB
  - docker SDK and IB test
  - docker SDK and IB deploy
  - docker IB LCM container
  - run
  - smoke tests
  - certification tests
  - cdrouter
  - coverity

build test mxl_x86_osp_tb341_v2 mxl_wlan_hostap_ng_wav700 prpl cellular security thread:
  extends: .build test config

build test mvebu prpl security:
  extends: .build test config

build test x86_64 prpl security:
  extends: .build test config

build test ipq807x prpl security:
  extends: .build test config

build test mtk_filogic prpl security:
  extends: .build test config

build test qca_ipq95xx prpl cellular security thread:
  extends: .build test config

coverity scan ipq807x prpl:
  extends: .coverity scan config
  variables:
    CI_COVERITY_COMPILER_TEMPLATE_LIST: >
      aarch64-openwrt-linux-gcc
      aarch64-openwrt-linux-musl-gcc

run test Turris Omnia initramfs:
  allow_failure: true
  needs: ["build test mvebu prpl security"]
  extends: .turris-omnia testbed

run test Haze initramfs:
  allow_failure: true
  needs: ["build test ipq807x prpl security"]
  extends: .prpl-haze testbed

run test MXL25641-HDK-6 with system on eMMC:
  needs: ["build test mxl_x86_osp_tb341_v2 mxl_wlan_hostap_ng_wav700 prpl cellular security thread"]
  extends: .mxl25641-hdk-6 testbed

run test Freedom initramfs:
  needs: ["build test qca_ipq95xx prpl cellular security thread"]
  extends: .wnc-freedom testbed

run test Mozart initramfs:
  needs:
    - job: build test mtk_filogic prpl security
      optional: true
  extends: .arcadyan-mozart testbed

".build test config":
  rules: [{ when: on_success }]
".turris-omnia testbed":
  rules: [{ when: never }]
".prpl-haze testbed":
  rules: [{ when: never }]
".cdrouter":
  rules: [{ when: never }]
".build Docker image":
  rules: [{ when: never }]
".docker in docker":
  rules: [{ when: never }]
".test Docker SDK image":
  rules: [{ when: never }]
".test Docker IB image":
  rules: [{ when: never }]
".deploy Docker image":
  rules: [{ when: never }]
".build LCM container":
  rules: [{ when: never }]
".test LCM container":
  rules: [{ when: never }]
".deploy LCM container":
  rules: [{ when: never }]
"CDRouter-Wireless Freedom initramfs": { rules: [{ when: on_success }] }
"CDRouter-Wireless MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"CDRouter-Wireless Mozart initramfs": { rules: [{ when: on_success }] }
"prplWare-Smoke-Test-Suite Freedom initramfs": { rules: [{ when: on_success }] }
"prplWare-Smoke-Test-Suite MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"prplWare-Smoke-Test-Suite Mozart initramfs": { rules: [{ when: on_success }] }
"EasyMesh-Smoke-test-R1-Freedom": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS Freedom VLAN WAN/LAN initramfs": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS Freedom initramfs": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS MXL25641-HDK-6 VLAN WAN/LAN eMMC": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS Mozart initramfs": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS Mozart VLAN WAN/LAN initramfs": { rules: [{ when: on_success }] }
"CDRouter-TR-069 Freedom eMMC": { rules: [{ when: on_success }] }
"CDRouter-TR-069 MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"CDRouter-TR-069 Mozart eMMC": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS Freedom PPPoE WAN initramfs": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS Freedom VLAN WAN/LAN initramfs": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS Freedom initramfs": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS Mozart PPPoE WAN initramfs": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS Mozart VLAN WAN/LAN initramfs": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS Mozart initramfs": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS-OSP MXL25641-HDK-6 PPPoE WAN eMMC": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS-OSP MXL25641-HDK-6 VLAN WAN/LAN eMMC": { rules: [{ when: on_success }] }
"CDRouter-Top-100+mDNS-OSP MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"CDRouter-UPnP-IPv46 Freedom initramfs": { rules: [{ when: on_success }] }
"CDRouter-UPnP-IPv46 MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"CDRouter-UPnP-IPv46 Mozart initramfs": { rules: [{ when: on_success }] }
"prplOS-Network-Test-Plan-Mandatory-v1.1.0 Freedom initramfs": { rules: [{ when: on_success }] }
"prplOS-Network-Test-Plan-Mandatory-v1.1.0 MXL25641-HDK-6 eMMC": { rules: [{ when: on_success }] }
"prplOS-Network-Test-Plan-Mandatory-v1.1.0 Mozart initramfs": { rules: [{ when: on_success }] }
"CDRouter-IPv6-Top-100+mDNS Haze initramfs": { rules: [{ when: never }] }
"CDRouter-IPv6-Top-100+mDNS Haze VLAN WAN/LAN initramfs": { rules: [{ when: never }] }
"CDRouter-IPv6-Top-100+mDNS Turris initramfs": { rules: [{ when: never }] }
"CDRouter-IPv6-Top-100+mDNS Turris VLAN WAN/LAN initramfs": { rules: [{ when: never }] }
"CDRouter-Top-100+mDNS Haze initramfs": { rules: [{ when: never }] }
"CDRouter-Top-100+mDNS Haze PPPoE WAN initramfs": { rules: [{ when: never }] }
"CDRouter-Top-100+mDNS Haze VLAN WAN/LAN initramfs": { rules: [{ when: never }] }
"CDRouter-Top-100+mDNS Turris initramfs": { rules: [{ when: never }] }
"CDRouter-Top-100+mDNS Turris PPPoE WAN initramfs": { rules: [{ when: never }] }
"CDRouter-Top-100+mDNS Turris VLAN WAN/LAN initramfs": { rules: [{ when: never }] }
"CDRouter-TR-069 Haze eMMC": { rules: [{ when: never }] }
"CDRouter-TR-069 Turris eMMC": { rules: [{ when: never }] }
"CDRouter-UPnP-IPv46 Haze initramfs": { rules: [{ when: never }] }
"CDRouter-UPnP-IPv46 Turris initramfs": { rules: [{ when: never }] }
"CDRouter-USP-prpl Freedom initramfs": { rules: [{ when: never }] }
"CDRouter-USP-prpl Haze initramfs": { rules: [{ when: never }] }
"CDRouter-USP-prpl Mozart initramfs": { rules: [{ when: never }] }
"CDRouter-USP-prpl MXL25641-HDK-6 eMMC": { rules: [{ when: never }] }
"CDRouter-USP-prpl Turris initramfs": { rules: [{ when: never }] }
"CDRouter-Wireless Haze initramfs": { rules: [{ when: never }] }
"CDRouter-Wireless Turris initramfs": { rules: [{ when: never }] }
"prpl-Certification Freedom initramfs": { rules: [{ when: never }] }
"prpl-Certification Haze initramfs": { rules: [{ when: never }] }
"prpl-Certification Mozart initramfs": { rules: [{ when: never }] }
"prpl-Certification MXL25641-HDK-6 eMMC": { rules: [{ when: never }] }
"prpl-Certification Turris initramfs": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Candidate-v1.1.0 Freedom initramfs": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Candidate-v1.1.0 Haze initramfs": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Candidate-v1.1.0 Mozart initramfs": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Candidate-v1.1.0 MXL25641-HDK-6 eMMC": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Candidate-v1.1.0 Turris initramfs": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Mandatory-v1.1.0 Haze initramfs": { rules: [{ when: never }] }
"prplOS-Network-Test-Plan-Mandatory-v1.1.0 Turris initramfs": { rules: [{ when: never }] }
"prplWare-Smoke-Test-Suite Haze initramfs": { rules: [{ when: never }] }
"prplWare-Smoke-Test-Suite Turris initramfs": { rules: [{ when: never }] }